#ifndef __UTILS__
#define __UTILS__

#include <stdlib.h>
#include <stdio.h>

int randint(int, int);
void display_array(int, char []);
void display_board(int, char (*)[]);
void mod(int *, int);
int min(int, int);
int max(int, int);

#endif
